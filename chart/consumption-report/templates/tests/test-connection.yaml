apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "consumption-report.fullname" . }}-test-connection"
  labels:
    {{- include "consumption-report.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "consumption-report.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
